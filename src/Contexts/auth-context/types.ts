// Shared values stored in auth-context
export type AuthContextValue = {
    isAuthed: Boolean
}


// Extra properties passed into wrapped components
export type ExtraWrappedComponentProps = {
    isAuthed: Boolean
}
